# Xcode project is generated from project.yml — run: xcodegen generate
*.xcodeproj

# Distribution archives (never commit)
*.zip

# Xcode user state and build artifacts
xcuserdata/
*.xcuserstate
DerivedData/
build/
*.ipa
*.dSYM.zip
*.dSYM
.DS_Store

# Server admin-tool secrets (OneSignal REST API key, admin password) —
# uploaded directly to the live server via FTP, never committed.
Server/app/config.local.php
